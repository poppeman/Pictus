#include "StdAfx.h"

SUITE(Geometry) {
	/*using namespace Geom;
	using namespace Filter;

	TEST(TestRotateRect) {
		RectInt rect(PointInt(0, 0), SizeInt(781, 1114));
		SizeInt dims(781, 1114);

		RectInt result = RotateRect(rect, dims, FlipY);
		CHECK(result == rect);
	}*/
}
